/** Placeholder for the per-node local structural diagram (sequence/choice box-and-connector view). */
export function LocalDiagram() {
  return (
    <div className="panel__body panel__body--empty">
      선택한 노드의 로컬 구조 다이어그램 (Phase 2)
    </div>
  );
}
